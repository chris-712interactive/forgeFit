/** Shared page padding for authenticated app views */
export const appPagePadding = "px-4 py-6 sm:px-6 sm:py-8";

/** Vertical rhythm between major visual sections (cards, banners, blocks) */
export const appSectionStack = "flex flex-col gap-6 sm:gap-8";

/** Tighter stack for repeated items within one section (e.g. workout day cards) */
export const appSectionStackTight = "flex flex-col gap-4 sm:gap-5";

/** Space between page title block and first content section */
export const appHeaderGap = "mt-6 sm:mt-8";
